name: example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_chen_kchart:
    path: ../
  get: ^4.6.6
  dio: ^5.4.0
  flutter_screenutil: ^5.9.3
  flutter_chen_common: ^2.9.0
  flutter_chen_refresh: ^3.0.0
  flutter_chen_generator: ^1.3.0
  flutter_chen_keyboard: ^1.0.0
  flutter_chen_azlist: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:

